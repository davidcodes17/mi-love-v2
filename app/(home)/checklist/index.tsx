import NativeText from '@/components/ui/native-text';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function Page() {
  return (
   <SafeAreaView>
    <NativeText>checklist</NativeText>
   </SafeAreaView>
  );
}
